{{> header.hbs }}

<section class="products">
	<h1>{{ title}}</h1>

	<p class="lead">So far I've released {{ Book.length }} short stories, which you can find on my <a href="http://author.to/stoo-goff">Amazon</a> page.</p>

	<div class="media">
		{{#each Book}}
			<a href="{{ ../baseUrl }}{{ path }}"><img src="{{ ../baseUrl }}{{ image.source }}" alt="{{ image.text }} " /></a>
		{{/each}}
	</div>
</section>

{{> footer.hbs }}